## Summary

<!-- What does this PR do? For protocol changes, reference the spec file. -->

## Checklist

- [ ] `npm test` passes
- [ ] `npm run typecheck` passes
- [ ] `npm run lint` passes
- [ ] `npm run check:surface` passes (or this PR updates `scripts/check-public-surface.ts` *and* the matching docs)
- [ ] `npm run check:facts` passes (documented numbers still match what the repository computes)
- [ ] Regression test added (for bug fixes)
- [ ] Spec updated if on-wire format or trust model changes
- [ ] No private keys, secrets, or Tulpa-internal details added
