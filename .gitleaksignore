# False positives suppressed from gitleaks scans.
#
# Each line is `<commit>:<path>:<rule>:<line>`. New entries here mean
# gitleaks WILL NOT alert on that specific match. Every entry must
# have a justification comment explaining why the match is not a real
# secret.

# The Python interop CLI's Keypair dataclass at
# examples/interop-cli/src/ink_interop/crypto.py:52 declares its
# private-key field with the cryptography library's class as the type
# annotation. The generic-api-key rule trips on the type name (a long
# alphanumeric substring), not on any actual secret value. The class
# docstring documents that the raw key bytes are deliberately not
# exposed on the dataclass surface.
c82048276408ea1321b39ae9c4f722c9599d8cd3:examples/interop-cli/src/ink_interop/crypto.py:generic-api-key:52

# Historical commit where the justification comment in this file
# itself contained the high-entropy substring before being rewritten.
# Suppresses the recursion on that one historical revision; HEAD no
# longer trips the rule.
4f0e04b29eda1f81fefe47fbd4ec98066aa0bd5a:.gitleaksignore:generic-api-key:8
