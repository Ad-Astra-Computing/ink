{
  "name": "@adastracomputing/ink-interop-lab-ts",
  "version": "0.0.0",
  "private": true,
  "description": "TypeScript side of the INK interop lab: the reference receiver plus the lab peer and driver, bundled for a container with no node_modules.",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "node build.mjs"
  },
  "devDependencies": {
    "esbuild": "0.28.1"
  }
}
