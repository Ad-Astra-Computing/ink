{
  "name": "@adastracomputing/ink-reference-receiver-example",
  "version": "0.0.0",
  "private": true,
  "description": "Publicly addressable INK receiver built solely on the @adastracomputing/ink public surface. Accepts foreign-agent envelopes and returns a plain JSON acknowledgement.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@adastracomputing/ink": "^0.2.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260610.1",
    "@types/node": "^22.19.20",
    "typescript": "^5.0.0",
    "vitest": "^4.1.8",
    "wrangler": "^4.99.0"
  }
}
